# Docker Compose Project Name
# Automatically generated from git remote URL by initialize.sh
# The initializeCommand in devcontainer.json runs this script before build
# Must start with letter/number and contain only lowercase alphanumeric, hyphens, underscores
COMPOSE_PROJECT_NAME=my-project

# 1Password Service Account Configuration
# Copy this file to .env and fill in your service account token
# Get your service account token from: https://my.1password.com/developer-tools/infrastructure-secrets/

OP_SERVICE_ACCOUNT_TOKEN="your-token-here"

# ============================================================================
# VPN Client Configuration (optional, multi-protocol)
# ============================================================================
# Built into base image: OpenVPN, WireGuard, IPsec/IKEv2, PPTP
# Auto-connects on container start via postStart.sh
#
# Format: op://VAULT/PROFILE
# Protocol detected from DOCUMENT item tags in 1Password:
#   - No tag = openvpn (backward compatible)
#   - "wireguard" tag = WireGuard (no LOGIN needed, keys in config)
#   - "ipsec" tag = IPsec/IKEv2 (StrongSwan)
#   - "pptp" tag = PPTP
#
# Each profile = items in vault with same title:
#   "PROFILE" (DOCUMENT) → config file
#   "PROFILE" (LOGIN)    → credentials (optional)
VPN_CONFIG_REF=op://VPN/HOME
